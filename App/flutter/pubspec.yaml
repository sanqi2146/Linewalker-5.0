name: linewalker
description: LineWalker 三合一智能小车控制APP (循迹+避障+蓝牙遥控)
publish_to: 'none'
version: 5.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.3.4
  path_provider: ^2.1.5
  permission_handler: ^11.3.1
  dynamic_color: ^1.7.0
  vibration: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
